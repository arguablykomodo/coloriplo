{
  "name": "coloriplo",
  "private": true,
  "scripts": {
    "bundle": "bun build index.html sw.ts --chunk-naming [name].[ext] --asset-naming [name].[ext] --outdir _site",
    "dev": "bun run bundle -- --watch --sourcemap",
    "build": "bun run bundle -- --minify && bun run make_icons.ts"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@melloware/coloris": "^0.25.0",
    "@tsparticles/confetti": "^3.9.1"
  }
}
